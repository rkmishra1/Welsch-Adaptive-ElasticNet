% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metrics.R
\name{run_simulation_cell}
\alias{run_simulation_cell}
\title{Run one design-grid cell of the simulation study}
\usage{
run_simulation_cell(n, p, rho, cov = "ar1", error = "clean", reps = 10L, ...)
}
\arguments{
\item{n}{Training sample size.}

\item{p}{Number of predictors.}

\item{rho}{Correlation parameter of the predictor covariance.}

\item{cov}{Covariance structure, `"ar1"` or `"cs"`, passed to
[make_dataset()].}

\item{error}{Contamination regime, passed to [make_dataset()].}

\item{reps}{Number of independent replications.}

\item{...}{Additional arguments passed to [fit_rbic()] (e.g. `l1_grid`,
`l2_grid`, `max_iter`).}
}
\value{
A one-row data frame with columns `TZ`, `FZ` (means over `reps`
  replications), `median_MSPE`, and `MSPE_SE` (standard error of the
  median, \eqn{1.2533 \times s / \sqrt{reps}}).
}
\description{
Repeatedly draws a dataset via [make_dataset()], fits [fit_rbic()], and
aggregates selection and prediction metrics -- the inner loop of the
manuscript's simulation study (Section 6). Competing estimators are out
of scope here (RBIC tunes W-AdEnet only); benchmark another `fit(X, y)`
callable by looping it alongside this function.
}
\examples{
\donttest{
set.seed(1)
run_simulation_cell(n = 60, p = 8, rho = 0.5, error = "clean", reps = 2,
                     l1_grid = c(0.1, 0.01), l2_grid = c(0, 0.1),
                     max_iter = 200)
}
}
