% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimator.R
\name{welsch_adenet}
\alias{welsch_adenet}
\title{Fit the Welsch adaptive elastic-net at one (lambda1, lambda2) pair}
\usage{
welsch_adenet(
  X,
  y,
  l1,
  l2,
  weights,
  sigma,
  c = 2.11,
  eta = 0.01,
  omega1 = 0.9,
  omega2 = 0.999,
  eps = 1e-08,
  beta0 = NULL,
  max_iter = 2000L,
  tol = 1e-06
)
}
\arguments{
\item{X}{Numeric design matrix, `n` by `p`.}

\item{y}{Numeric response vector, length `n`.}

\item{l1}{Non-negative scalar, the adaptive lasso penalty \eqn{\lambda_1}.}

\item{l2}{Non-negative scalar, the ridge penalty \eqn{\lambda_2}.}

\item{weights}{Numeric vector of length `p`, the adaptive weights
\eqn{\hat w_j} (see [adaptive_weights()]).}

\item{sigma}{Positive scalar, the robust scale estimate \eqn{\hat\sigma}
(see [robust_init()]).}

\item{c}{Positive scalar, the Welsch tuning constant (default `2.11`,
approximately 95\% Gaussian efficiency).}

\item{eta}{Numeric scalar, the Adam base step size.}

\item{omega1, omega2}{Numeric scalars in `[0, 1)`, the Adam first- and
second-moment decay rates.}

\item{eps}{Small positive constant preventing division by zero in the
Adam step.}

\item{beta0}{Optional numeric warm start of length `p` (defaults to a
zero vector).}

\item{max_iter}{Maximum number of proximal Adam iterations.}

\item{tol}{Relative loss-change convergence tolerance.}
}
\value{
Numeric vector of length `p`, the rescaled coefficient estimate
  \eqn{(1 + \lambda_2/n) \beta^\star} that undoes the elastic-net's
  double shrinkage.
}
\description{
Minimizes the (non-convex) Welsch data-fit term plus a convex adaptive
elastic-net penalty by a proximal Adam scheme: an Adam gradient step on
the smooth Welsch loss, followed by the closed-form soft-threshold /
ridge-shrink proximal map of the penalty. This is the core solver
(Algorithm 5.1 in the accompanying manuscript); see [fit_rbic()] to also
select `(l1, l2)` automatically.
}
\examples{
set.seed(1)
X <- matrix(rnorm(100 * 5), 100, 5)
beta_star <- c(2, -1.5, 0, 0, 1)
y <- as.numeric(X \%*\% beta_star) + rnorm(100)

init <- robust_init(X, y)
w <- adaptive_weights(init$beta0)
beta_hat <- welsch_adenet(X, y, l1 = 0.05, l2 = 0.01, weights = w,
                           sigma = init$sigma)
round(beta_hat, 2)
}
\seealso{
[fit_rbic()] for automatic penalty selection by robust BIC,
  [robust_init()] and [adaptive_weights()] for the required `sigma` and
  `weights` inputs.
}
