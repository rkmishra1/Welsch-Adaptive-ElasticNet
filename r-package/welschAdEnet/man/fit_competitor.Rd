% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/competitors.R
\name{fit_competitor}
\alias{fit_competitor}
\title{Fit a penalized robust-regression competitor}
\usage{
fit_competitor(
  X,
  y,
  l1,
  l2,
  weights,
  sigma,
  loss_type = "squared",
  eta = 0.01,
  omega1 = 0.9,
  omega2 = 0.999,
  eps = 1e-08,
  beta0 = NULL,
  max_iter = 2000L,
  tol = 1e-06,
  loss_param = NULL
)
}
\arguments{
\item{X}{Numeric design matrix, `n` by `p`.}

\item{y}{Numeric response vector, length `n`.}

\item{l1}{Non-negative scalar, the adaptive lasso penalty \eqn{\lambda_1}.}

\item{l2}{Non-negative scalar, the ridge penalty \eqn{\lambda_2}.}

\item{weights}{Numeric vector of length `p`, the adaptive lasso weights.}

\item{sigma}{Positive scalar, the robust scale estimate.}

\item{loss_type}{One of `"squared"`, `"absolute"`, `"huber"`, `"tukey"`,
or `"welsch"`.}

\item{eta}{Numeric scalar, the Adam base step size.}

\item{omega1, omega2}{Numeric scalars in `[0, 1)`, the Adam first- and
second-moment decay rates.}

\item{eps}{Small positive constant preventing division by zero in the
Adam step.}

\item{beta0}{Optional numeric warm start of length `p` (defaults to a
zero vector).}

\item{max_iter}{Maximum number of proximal Adam iterations.}

\item{tol}{Relative loss-change convergence tolerance.}

\item{loss_param}{Optional tuning constant for the chosen loss (`k` for
Huber, `d` for Tukey, `c` for Welsch); `NULL` uses each loss's usual
default constant.}
}
\value{
Numeric vector of length `p`, the rescaled coefficient estimate.
}
\description{
A single proximal Adam solve generalizing [welsch_adenet()] to a
selectable robust loss, so that alternative estimators can be benchmarked
against the Welsch adaptive elastic-net with the same solver and adaptive
elastic-net penalty.
}
\examples{
set.seed(1)
X <- matrix(rnorm(80 * 4), 80, 4)
y <- as.numeric(X \%*\% c(1, -1, 0, 0)) + rnorm(80)
init <- robust_init(X, y)
w <- adaptive_weights(init$beta0)
beta_hat <- fit_competitor(X, y, l1 = 0.05, l2 = 0.01, weights = w,
                            sigma = init$sigma, loss_type = "huber")
round(beta_hat, 2)
}
\seealso{
[tune_competitor()] to also select `(l1, l2)` by robust BIC,
  [welsch_adenet()] for the Welsch-specific solver this generalizes.
}
