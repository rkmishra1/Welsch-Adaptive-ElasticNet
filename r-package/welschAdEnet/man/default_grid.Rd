% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning.R
\name{default_grid}
\alias{default_grid}
\title{Default (lambda1, lambda2) penalty grid}
\usage{
default_grid(X, y, sigma, n_l1 = 20L, n_l2 = 5L)
}
\arguments{
\item{X}{Numeric design matrix, `n` by `p`.}

\item{y}{Numeric response vector, length `n`.}

\item{sigma}{Positive scalar, the robust scale estimate.}

\item{n_l1, n_l2}{Sizes of the \eqn{\lambda_1} and \eqn{\lambda_2} grids.}
}
\value{
A list with numeric elements `l1_grid` and `l2_grid`.
}
\description{
A data-driven, log-spaced \eqn{\lambda_1} grid -- from the smallest
penalty that zeroes every coefficient at `beta = 0` (a KKT-style
\eqn{\lambda_{\max}}) down to 0.1\% of that value -- crossed with a small
\eqn{\lambda_2} grid.
}
\examples{
set.seed(1)
X <- matrix(rnorm(60 * 3), 60, 3)
y <- as.numeric(X \%*\% c(1, 0, -1)) + rnorm(60)
default_grid(X, y, sigma = 1, n_l1 = 5, n_l2 = 3)
}
