% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning.R
\name{rbic_score}
\alias{rbic_score}
\title{Robust BIC (RBIC) score}
\usage{
rbic_score(X, y, beta, sigma, c)
}
\arguments{
\item{X}{Numeric design matrix, `n` by `p`.}

\item{y}{Numeric response vector, length `n`.}

\item{beta}{Numeric coefficient vector, length `p`.}

\item{sigma}{Positive scalar, the robust scale estimate.}

\item{c}{Positive scalar, the Welsch tuning constant.}
}
\value{
A single numeric RBIC score.
}
\description{
Robust BIC objective: the summed Welsch deviance of the standardized
residuals plus a BIC-style penalty on the number of active (non-zero)
coefficients. Lower is better; see [fit_rbic()], which minimizes this
score over a `(lambda1, lambda2)` grid.
}
\examples{
set.seed(1)
X <- matrix(rnorm(60 * 3), 60, 3)
y <- as.numeric(X \%*\% c(1, 0, -1)) + rnorm(60)
rbic_score(X, y, beta = c(1, 0, -1), sigma = 1, c = 2.11)
}
