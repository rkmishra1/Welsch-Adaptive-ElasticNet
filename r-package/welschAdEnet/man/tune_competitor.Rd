% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/competitors.R
\name{tune_competitor}
\alias{tune_competitor}
\title{Tune a competitor loss by grid search over robust BIC}
\usage{
tune_competitor(
  X,
  y,
  loss_type = "squared",
  loss_param = NULL,
  gamma = 1,
  n_l1 = 20L,
  n_l2 = 5L,
  l1_grid = NULL,
  l2_grid = NULL,
  ...
)
}
\arguments{
\item{X}{Numeric design matrix, `n` by `p`.}

\item{y}{Numeric response vector, length `n`.}

\item{loss_type}{One of `"squared"`, `"absolute"`, `"huber"`, `"tukey"`,
or `"welsch"`.}

\item{loss_param}{Optional tuning constant for the chosen loss; see
[fit_competitor()].}

\item{gamma}{Adaptive-weight exponent. Ignored for
`loss_type = "absolute"`, which uses uniform weights (matching the
usual non-adaptive LAD-lasso).}

\item{n_l1, n_l2}{Sizes of the default `(lambda1, lambda2)` grids, used
only when `l1_grid`/`l2_grid` are `NULL`.}

\item{l1_grid, l2_grid}{Optional numeric penalty grids.}

\item{...}{Additional arguments passed to [fit_competitor()].}
}
\value{
A list with elements `beta`, `lambda1`, `lambda2`, `sigma`,
  `weights`, and `rbic` (the best score found).
}
\description{
Grid search over `(lambda1, lambda2)` for a given `loss_type`, minimizing
a robust-BIC-style score (the summed loss over standardized residuals
plus a BIC complexity penalty on the active set), analogous to
[fit_rbic()] but for the competing losses in [fit_competitor()].
}
\examples{
\donttest{
set.seed(1)
X <- matrix(rnorm(80 * 4), 80, 4)
y <- as.numeric(X \%*\% c(1, -1, 0, 0)) + rnorm(80)
res <- tune_competitor(X, y, loss_type = "huber",
                        l1_grid = c(0.1, 0.02), l2_grid = c(0, 0.1))
round(res$beta, 2)
}
}
