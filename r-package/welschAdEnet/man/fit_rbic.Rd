% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tuning.R
\name{fit_rbic}
\alias{fit_rbic}
\title{Fit W-AdEnet, selecting penalties by robust BIC}
\usage{
fit_rbic(X, y, c = 2.11, gamma = 1, l1_grid = NULL, l2_grid = NULL, ...)
}
\arguments{
\item{X}{Numeric design matrix, `n` by `p`.}

\item{y}{Numeric response vector, length `n`.}

\item{c}{Positive scalar, the Welsch tuning constant (default `2.11`).}

\item{gamma}{Adaptive-weight exponent, passed to [adaptive_weights()].}

\item{l1_grid, l2_grid}{Optional numeric penalty grids. If either is
`NULL`, both default to [default_grid()] with its default sizes (20 and
5 respectively).}

\item{...}{Additional arguments passed to [welsch_adenet()] (e.g. `eta`,
`max_iter`, `tol`).}
}
\value{
A list with elements `beta` (the selected coefficient vector),
  `lambda1`, `lambda2`, `sigma`, `weights`, `rbic` (the best score), and
  `path` (a matrix with one row per grid point visited, columns `l1`,
  `l2`, `rbic`).
}
\description{
Runs [robust_init()] and [adaptive_weights()] to obtain a warm start,
scale estimate, and adaptive weights, then performs a warm-started 2-D
grid search over `(lambda1, lambda2)`, refitting [welsch_adenet()] at
each grid point and keeping the fit with the lowest [rbic_score()]. Per
the manuscript design, RBIC tunes the Welsch adaptive elastic-net only;
see [tune_competitor()] for the analogous procedure applied to other
robust losses.
}
\examples{
\donttest{
set.seed(1)
n <- 150
p <- 20
beta_star <- c(2, -1.5, 1, rep(0, p - 3))
X <- matrix(rnorm(n * p), n, p)
y <- as.numeric(X \%*\% beta_star) + rnorm(n)

res <- fit_rbic(X, y, l1_grid = c(0.2, 0.05, 0.01), l2_grid = c(0, 0.1))
round(res$beta, 2)
res$lambda1
res$lambda2
}
}
